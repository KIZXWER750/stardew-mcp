# 1.11.2 검증 기록 — 2026-09-19

## 게임 없이 완료한 검증

- 기존 농사·물 보충·상점·판매·상자·OpenAI 공급자 Go 테스트 유지.
- 정상 도구 48개 등록, 생활 관리자와 일반 야생 나무 제거 등록, 치트 도구 비노출 확인.
- Go 형식 검사, 단위 테스트, 정적 검사, Windows amd64 크로스 빌드.
- 모든 C# 소스의 문자열·주석 인식 구분자 정적 검사 성공. 실제 게임 참조 컴파일은 게임 PC 설치 단계에서 수행.
- 음식 섭취는 정확한 슬롯/ID/양수 회복값/보존 수량을 검사하고, 한 개의 수량 감소와 회복 전후 상태를 결과로 반환하도록 구현.
- 귀가는 매 전환마다 현재 위치에서 `FarmHouse` 경로를 다시 읽고, 반환된 접근점과 목적지 전환을 검증하도록 구현.
- 수면은 기존에 검증했던 실제 침대 탐색, 좌우 접근, 수면 질문 확인, 다음 날 아침 확인 코드를 일반 AI 함수로 공개.
- `Farm -> FarmHouse` 맵 간선이 누락될 때만 표준 정문 `(64,14)`과 남쪽 접근 `(64,15)`을 추가하는 귀가 경로 보완. 정상 이동과 위치 전환 검증은 유지.
- 농가 현관의 실제 통행 가능 구간 `x=59..66, y=15`와 `x=63..65, y=16`을 경로 탐색 예외로 추가. 객체·지형·자원 덩어리 충돌 검사는 유지.
- `return_home`을 현재 위치→Farm 경로와 Farm→정문 전용 입력으로 분리하고 각 지역 전환을 별도로 검증.
- 현관 외부/내부 경계 통과는 `(63..65,17)`에서 북쪽 `(63..65,16)`으로 들어가는 세 간선과 그 역방향만 허용. 벽을 통과하는 동·서·북 접근 차단.
- OpenAI 도구 라운드 180회, 상태 재관측 180회, 목표 문맥 20분으로 확장. 기존 중복·농사·거래 안전 제한 유지.
- 생활 관리자 정책 기본값·잘못된 입력 차단·판매가/예비 수량/보호 ID 기반 음식 선택 단위 테스트.
- 나무 작업의 수량·어린 나무 보존 정책을 중복 방지 키에 포함하고 최대 12그루 제한 검증.
- `preserve_young_trees` 생략 시 false가 전달되어 일반 야생 나무의 모든 성장 단계가 기본 제거 대상이 되는지 단위 테스트.
- 주변 상태가 map object와 별도로 실제 `location.debris`의 좌표·아이템 ID·수량·청크 수를 직렬화하도록 수정.
- 성목 10회, 밑동 5회, 어린나무 4회의 단계별 타격 한도와 성목→밑동 전환 시 카운터 초기화 구현.
- 나무 완료 후 드롭 안정화 대기, 최대 8타일 드롭 검색, 정상 이동 회수, 인벤토리 증가 및 드롭 소멸 증거 기록 구현.
- 드롭 접근 경로 정리 시 일반 나무·잔디·잡초·작은 돌·잔가지만 허용하고 과일나무·작물·경작지·시설을 제외하도록 구현.
- Go 단위 테스트, vet, race 테스트, Windows amd64 서버 크로스 빌드 완료.

## 게임 PC에서 반드시 검증할 내용

- C# 실제 Stardew Valley/SMAPI 참조 빌드와 설치.
- `assess_daily_status`, 음식 목록의 회복량·판매가 표시.
- 음식 확인창이 실제로 `Eat` 질문으로 식별되고 한 개만 소비되는지.
- Farm, Town, SeedShop 등 서로 다른 위치에서 `return_home`이 출구를 순서대로 통과해 FarmHouse에 도착하는지.
- 침대 배치가 다른 세이브에서도 좌우 접근과 다음 날 검증이 동작하는지.
- F7 취소 및 작업 완료 후 다음 F6 명령 실행 회귀.
- 저에너지 상태에서 허용된 저가 음식만 정해진 개수 이내로 먹고 `resumeRequestedWork`를 정확히 반환하는지.
- 22:00 이후 귀가·수면 허용 조합에 따라 FarmHouse 도착 또는 다음 날 아침에서 정확히 멈추는지.
- 일반 어린나무와 성목을 별도 지시 없이 모두 대상으로 선택하는지, 성목은 동일 좌표의 밑동까지 제거하는지, 과일나무·작물·시설을 보존하는지.
- `preserve_young_trees=true`일 때 어린 일반 나무는 제외하고 성목과 기존 밑동만 처리하는지.
- 일반 도끼로 성목이 10회 이내에 쓰러지고 밑동이 새 5회 한도 안에서 제거되는지. 업그레이드 도끼는 더 적은 타격에서 즉시 단계 전환하는지.
- 목재·수액·씨앗 드롭이 `nearbyDebris`에 `source=location_debris`로 보이고, 나무 작업 결과의 드롭 개수·인벤토리 증가와 일치하는지.
- 드롭이 잡초·잔디·작은 돌·잔가지 뒤에 있을 때 해당 장애물만 제거하고 회수하는지. 과일나무·작물·상자·기계가 막으면 `DROP_UNREACHABLE_PROTECTED_PATH`로 중단하는지.
- 여러 나무 작업에서 `max_trees`를 초과하지 않고 저에너지·시간 제한 시 부분 진행으로 중단하는지.

목욕탕 자동 회복은 이번 버전의 실행 함수가 아닙니다. 나무 드롭 수집은 `remove_wild_trees`의 필수 후속 단계로 포함됩니다. 실제 게임 참조 컴파일과 인게임 행동을 이 환경에서는 검증할 수 없으므로, 실패 시 반복 실행하지 말고 로그와 정확한 `TASK_BLOCKED` 결과를 보존해야 합니다. `TESTING.md`는 생성하지 않았습니다.
