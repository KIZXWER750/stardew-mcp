# 1.19.0 검증 기록 — 2026-09-20

1.19.0은 저장된 돈 목표 계획을 고유 lease가 있는 한 단계씩 실행합니다. 구매·경작·파종·물주기·수확·판매 결과를 실제 돈, 인벤토리와 고정 농장 영역에서 다시 검증하고 다음 날짜의 단계는 자동 재개합니다. 목표 저장 schema는 3이며 이전 실행 전 계획은 안전한 재계획을 한 번 요구합니다.

## 1.19.0 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 84개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `6C761395DE2BDA05BB887EC076554353B4056A90198F7EFC969E5E8FB08F5254`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사 50개 통과: schema 1→3 변환, schema 2 계획의 재계획 강제, 고정 밭과 진행 중 lease JSON 복원, 계획 면적 계산을 포함합니다.
- Go 계약 검사는 84개 도구 등록, 한 단계 lease, 완료 보고, 고정 밭 바인딩과 다음 날 자동 재개 연결을 확인합니다.
- 자동 검사는 실제 메뉴 이동과 여러 게임 날짜를 진행하지 않습니다. 구매·경작·파종·물주기·수확·판매와 게임 재실행 후 자동 재개는 `INGAME_TESTS_1.19.0.md`의 프롬프트로 확인해야 합니다.

## 1.18.0 검증 기록

1.18.0은 목표별 전략 선택, 날짜·의존관계가 있는 영속 계획, 경제 상태 지문 기반 계획 무효화, 전용 질문 답변에서 검증된 행동 권한 반영을 추가합니다. 계획은 실제 게임 행동을 실행하지 않습니다. 목표 저장 schema를 2로 올리고 기존 schema 1을 자동 변환합니다.

## 1.18.0 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 80개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `616F60E92BF69DB04E4C96D1B2F1234BC9E168B9D77027C79C248C966C106AB9`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사는 전략 선호도 선택과 schema 1→2 변환을 포함한 45개입니다.
- Go 계약 검사는 실제 도구 80개와 계획 비실행·전용 질문 연계 계약을 확인합니다.
- 실제 게임 통합 확인에서는 계획 저장 후 재실행 복원, 돈/날짜/인벤토리 변경 시 `stale`, 질문 허용 후 재계획을 확인해야 합니다.

## 1.17.0 검증 기록

1.17.0은 기능 지원표, 경제 상태 통합 관측, 설치 데이터 기반 작물 수익 계산, 목표별 수익 후보와 `restore_tilled_soil`을 추가합니다. 경작지 복원은 빈 `HoeDirt`만 대상으로 곡괭이 정상 입력을 보내고 실제 제거를 검증하며 작물·객체는 제외합니다. 경제 분석은 읽기 전용이고 자동 전략 선택·실행은 하지 않습니다. C# 회귀 검사는 단일 수확·재수확·계절 내 수확 불가 계산을 포함한 41개를 실행하며, Go 계약 검사는 76개 실제 도구 등록과 정상 곡괭이 입력 계약을 확인합니다.

## 1.17.0 이번 PC에서 통과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 76개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `407679652F5CE8DE00DE06DD87E098207819427AF8F5AB61EE8879384C8A2B45`.
- C# 정책·메모·지식·목표·경제 계산 회귀 검사 41개 통과.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- 실제 게임 실행 통합 검증은 별도입니다. 설치 후 첫 인게임 확인에서는 빈 경작지·작물 있는 경작지·일반 땅이 섞인 영역을 사용해 대상 제외와 상태 검증을 확인해야 합니다.

## 1.16.0 이하 검증 기록

1.16.0은 `goals-v1` 주·백업 저장, 돈 목표 상태 전이, 현재 잔액·순잔액 증가 검증, 마감 경과 판정, 관련 메모 문맥의 활성 목표 주입, 전용 사용자 질문창과 답변 후속 실행, 오른쪽 아래 진행 HUD를 추가합니다. C# 회귀 검사는 목표 계산·상태 전이·JSON 왕복을 포함한 38개를 실행하고, Go 계약 검사는 71개 실제 도구 등록과 장기 목표·전용 질문 UI 계약을 확인합니다.

1.15.0은 장기 메모 선별 주입, 세이브 로드·날짜 변경 복원 감사, 실제 맵·상점 입력 기반 지식 캐시 무효화, 구조화된 위키 지식, 나무 정상 입력 타격을 통합합니다. 정책 검사는 목표·장소별 메모 선별, 완료 작업 제외, 사용자 규칙 유지, JSON 왕복 뒤 revision·안정 ID 보존, 맵 입력 변화에 따른 SHA-256 서명 변경을 확인합니다. Go 계약 검사는 선택 메모가 한 번만 데이터로 주입되는지와 나무 타격이 정상 입력 분기를 사용하는지 확인합니다.

1.14.3은 나무 관측에 `treeState`, `isStump`, `hitsRequired`, `estimatedTotalHitsToRemove`, `removalSequence`를 추가합니다. 밑동을 성목으로 표시하던 `isFullyGrown`/`canBeChopped` 판정을 수정하고, 일반 `clear_target`이 나무를 한 단계만 처리한 뒤 완료로 보고하지 못하게 차단합니다. Go 회귀 테스트는 JSON 역직렬화부터 실제 AI 문맥 문자열까지 상태 정보가 보존되는지 확인합니다.

1.14.2는 다음 날 로드 후 `Game1.newDay`가 참인 6시 입력 게이트에서도 정상 이동 입력을 보내도록 수정합니다. 이전 구현은 이 값이 참이면 입력 전송 전에 반환했기 때문에 사용자의 클릭이 성공으로 오인될 수 있었습니다. 새 구현은 네 방향을 순환하며 입력하고 `hasMoved`와 `shouldTimePass()`를 함께 확인하며, `MORNING GATE/INPUT/VERIFIED` 로그를 남깁니다.

1.14.1 핫픽스는 일반 나무의 `growthStage`, `isFullyGrown`, `canBeChopped`를 AI 문맥에 명시하고, 과일나무를 `find_best_target(tree)` 후보에서 제외하며, 대상이 없는 `remove_wild_trees`를 `NO_ELIGIBLE_TREE`로 차단합니다. 실제 실패 좌표 `(43,13)`처럼 성장 단계 5 미만인 나무를 성목으로 오인하는 회귀 테스트를 추가했습니다.

이전 공유 대화의 다섯 가지 요구사항을 로컬 1.13.6 소스에 이어 구현했습니다. 이전 환경에서 작성했다는 1.14.0 소스/ZIP은 이 PC에 없어, 그 바이너리를 검증했다고 간주하지 않습니다.

## 이번 PC에서 통과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l` 검사, `go test ./...`, `go vet ./...` 통과.
- Windows amd64 Go 서버 빌드 통과. SHA256: `3598FD0E61BF99E1D1C4F591CE958D38C9780CA3D21561192C5D556F6889D702`.
- 게임 코드와 공유하는 C# 정책·메모·지식 회귀 검사 30개 통과: 최단 경로/가중 경로 차이, 보호 벽, 현관 간선 제한, 경계 및 이미 도착한 상태, F7 취소, 에너지 부족, 야간 제한, 같은 날 시간 초과 반복 방지, 다음 날 재개, 충분한 회복과 미미한 회복 구분, 관련 메모 선별, 완료 작업 제외, JSON 왕복 복원, 맵 입력 변경 시 지식 서명 무효화.
- 실제 설치된 Stardew Valley/SMAPI 참조로 C# 모드 빌드 성공: 오류 0개, 경고 8개. 폐기 예정 WebSocket API, nullable, 기존 Count 메서드 그룹과 미사용 변수 경고 포함. 경고를 숨기지 않았습니다.
- 기존 Go 테스트의 농사·거래·상자·시간 알람·AI 공급자·치트 비노출 계약 유지.
- 로컬 게임 어셈블리의 LevelUpMenu/ShippingMenu 정상 확인 경로를 읽고 사용했습니다. LevelUpMenu.receiveLeftClick이 비어 있어 정상 okButtonClicked 처리 및 실제 입력을 사용합니다.

## 자동 테스트가 보장하지 않는 범위

- 실제 게임을 실행한 통합 테스트는 수행하지 않았습니다. 모드를 설치하거나 세이브를 수정하지 않았고 유료 AI API를 호출하지 않았습니다.
- `go test -race`는 이 PC에 CGO용 C 컴파일러가 없어 실행하지 못했습니다. 기존 문서의 과거 race 통과를 이번 실행 결과로 재사용하지 않았습니다.
- 배포 ZIP은 소스·검증 스크립트·Go exe를 포함하고, 게임 DLL·빌드 캐시·개인 설정·로그·세이브별 체크포인트는 제외합니다. C# 모드는 설치 시 게임 참조로 다시 빌드합니다.

## 인게임 확인 항목

1. 일반 스킬 상승과 배송 정산 화면을 연속 처리하고 저장 완료를 기다리는지. 직업 선택은 자동 선택 없이 명확히 중단하는지.
2. 다음 날 정상 입력으로 기상 대기가 해제되고 실제로 움직일 수 있는지. 알 수 없는 이벤트/메뉴는 임의로 닫지 않는지.
3. 여러 성목에서 한 그루씩 나무→밑동→드롭 순서를 지키는지. 드롭 개수와 인벤토리 증가가 일치하는지.
4. 먼 드롭 우선 수거와 짧은 장애물 제거 경로를 선택하는지. 보호 대상·도구 부족·장애물 예산·에너지/시간 제한을 지키는지.
5. 저에너지·시간·알람 중단이 세이브/플레이어별로 보존되고, 회복/다음 날 AI 재관측 후 원래 좌표만 재개하는지. F7 이후에는 자동 재개하지 않는지.
6. 기존 농사·상자 수량 보존·열기/닫기·F6 새 목표·F7 취소 회귀.

인게임 오류가 있으면 동일 행동을 반복하기보다 정확한 TASK_BLOCKED 결과와 로그를 보존해야 합니다.
