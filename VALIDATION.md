# 1.9.2 검증 기록 — 2026-09-18

## 게임 없이 검증

- 기존 농사/물 보충/상점/판매/상자/목표 종료 단위 테스트 유지.
- OpenAI 요청의 모델 ID, medium 고정, store=false, 동시 호출 금지 확인.
- 실제 등록된 정상 도구 37개의 스키마 로딩과 치트 제외 확인.
- 모의 API → 함수 실행 → 함수 결과/암호화 reasoning 전달 → 최종 종료 검증.
- 동일 call_id 재전달 시 재실행 방지, 인수 변경 재사용 차단.
- 잘못된 도구/타입/추가 인수/null/누락 ID 및 취소 상태에서 실행 차단.
- HTTP 301/400/401/403/404/429/500/503 자동 재시도 없음, 오류 본문/키 비노출.
- 비정상 JSON, 불완전 응답, 병렬 도구 응답, 빈 최종 응답, 60회 제한 차단.
- Go 정적 검사와 Windows amd64 크로스 빌드.
- `go test -race -count=1 ./...`로 경쟁 상태 검사 포함.
- 실행 파일의 키 유무 설정 검사와 JSON-lines 호스트의 키 누락 거부/정상 종료 확인. 가짜 키로만 검사했으며 네트워크 호출은 하지 않음.
- OpenAI 시작 경로에서 Copilot CLI를 실행하지 않는 것과 도구 결과/최종 응답의 키 마스킹 확인.
- 선택 함수 인수의 `null` 생략 처리와, 잘못된 인수가 실제 게임 행동 없이 교정된 다음 정확히 한 번만 실행되는 흐름 확인.
- 피에르 판매 카운터 `(4,18)`와 남쪽 접근 칸 `(4,19)`의 고정 계약 및 `open_pierre_shop` 등록 확인. 실제 메뉴 개방은 게임 PC 검증 대상.

## 미검증 — 게임 PC에서 남은 단계

- C# 실제 게임/SMAPI 참조 빌드 및 Windows 설치 스크립트 실행.
- 실제 OpenAI API 계정의 Luna 접근 권한·과금·응답 호환성. 이 작업에서는 실제 키가 없으며 유료 요청을 보내지 않았습니다.
- 게임 프로세스 환경변수 상속, F6 결과 표시, F7 취소, 다음 명령 재사용.
- 실제 농사/자동 보충/상점/상자 기능의 회귀 검증.

기존 C# 행동 코드는 변경하지 않았습니다. C# 변경은 버전과 호스트 준비 상태 문구 표시뿐입니다. 테스트 통과는 게임 API 타입 호환성·실제 도구 애니메이션·모델 판단의 정확성을 보증하지 않습니다. TESTING.md는 생성하지 않았습니다.
