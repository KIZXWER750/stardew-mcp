# 1.20.0 검증 기록 — 2026-09-20

1.20.0은 로그에서 확인된 과도한 질문, 작물 성장일 센티널 계산, 성숙 작물 재물주기, 상점 영업시간 자동 재개, 수확 방식, 잔여 에너지 사용, 구매 시 인벤토리 포화 문제를 수정합니다.

## 1.20.0 검증 항목

- 허용된 씨앗 종류·수량·작물 관리·상점 대기·일반 판매 선택 질문은 전용 질문창 대신 `AUTONOMOUS_DECISION_REQUIRED`로 AI가 직접 결정합니다.
- `[1,1,1,1,99999]` 성장 단계는 28일이 아니라 실제 4일로 계산됩니다.
- 저장된 스키마 v3 농사 계획은 로드 시 `CROP_SCHEDULE_MIGRATION_REPLAN_REQUIRED`로 전환됩니다.
- 물주기 전에 실시간 성숙 상태를 검사하고, 성숙 작물은 남은 물주기를 건너뛰어 즉시 수확 단계로 이동합니다.
- 작물별 `Grab`/`Scythe` 수확 방식이 관측·계획·실행 지침에 포함됩니다.
- 날짜·시각·최소 체력·위치 조건을 가진 일회성 AI 재호출이 저장되고 재시작 후에도 실행됩니다.
- 피에르 영업 전 일시정지 단계는 같은 날 오전 9시 이후 다시 자동 배정됩니다.
- 허용된 정리 작업은 기본적으로 최대 체력의 약 50%까지 이어집니다.
- 피에르 구매 중 인벤토리 포화 시 보호 품목을 제외한 최저 단가 판매 가능 스택을 하나 판매해 빈 칸을 검증합니다.
- 정책·메모리·지식·목표·경제 회귀 검사 64개와 Go 계약/도구 검사를 실행합니다.

## 1.19.9 검증 기록

1.19.9는 답변 완료된 전용 질문과 동일한 질문을 새 ID로 반복 생성하던 문제를 수정합니다.

## 1.19.9 검증 항목

- 질문 지문은 공백과 문장 부호 차이를 무시합니다.
- 같은 행동 이름을 포함한 권한 질문은 어순이 달라도 동일 질문으로 판정합니다.
- 답변된 질문과 동일한 대기 질문은 로드 시 `duplicate_suppressed`로 기록되고 UI에서 제거됩니다.
- 동일 질문의 새 요청은 `ALREADY_ANSWERED`와 기존 답변을 반환하며 새 질문창을 만들지 않습니다.
- 권한이 이미 저장된 상태에서 계획 후보가 없으면 동일 권한 질문 대신 자금·예비금·계절 조건에 대한 새 질문을 만듭니다.
- Go 전체 테스트, C# 정책 검사 58개와 실제 게임 참조 빌드를 통과해야 합니다.

## 1.19.8 검증 기록

1.19.8은 계절 전환 뒤 시든 작물을 살아 있는 작물처럼 물주던 문제를 수정하고, 시든 작물 제거 단계·아침 좌클릭 입력·왼쪽 계획 진행 HUD를 추가합니다.

## 1.19.8 검증 결과

- Go 전체 테스트 통과. `remove_dead_crops` 도구 등록, 시든 작물 선행 제거 규칙, 계획 단계 순서를 검사했습니다.
- C# 정책 검사 55개 통과.
- 실제 설치 게임 참조 Release 빌드 성공: 오류 0개, 기존 경고 8개.
- 인게임에서는 시든 작물이 낫으로 제거되고 살아 있는 인접 작물이 유지되는지, 수면 뒤 `[MORNING CLICK]` 다음 시간이 610 이상으로 진행되는지 확인해야 합니다.
- 계획 HUD는 왼쪽 반투명 검은 배경에 현재·예정 단계를 흰색으로, 완료 단계를 노란색으로 표시합니다.

## 1.19.7 검증 기록

1.19.7은 실제 로그에서 오전 6시에 `hasMoved=true`, `shouldTimePass=true`만 확인하고 시계가 `600`인 채 수면을 완료 처리한 문제와, FarmHouse 안에서 물주기를 먼저 호출한 실패가 `PREVIOUS_FAILED`로 고정된 문제를 수정합니다.

## 1.19.7 검증 결과

- Go 전체 테스트 통과. 잘못된 위치의 Farm 함수가 `actionMayHaveExecuted=false`인 선행 조건 결과를 반환하고 Farm 위치에서는 통과하는지 확인했습니다.
- 수면 계약 검사는 실제 위치 변화와 `Game1.timeOfDay > morningStartTime`을 모두 요구합니다.
- 자동 계획 재개 계약은 FarmHouse에서 Farm 단계 임대 전에 `exit_house` 호출을 요구합니다.
- C# 회귀 검사 55개 통과, 실제 설치 게임 참조 빌드 성공: 오류 0개, 기존 경고 8개.
- 인게임에서는 수면 뒤 `[MORNING MOVEMENT VERIFIED]`와 시간이 `610` 이상인 `[MORNING VERIFIED]`, Farm 이동 후 물주기 실행 순서를 확인해야 합니다.

## 1.19.6 검증 기록

1.19.6은 미래 날짜 계획을 저장한 뒤 `GOAL WAITING`으로 종료하지만 날짜를 넘길 실행이 없어 게임이 그대로 남던 문제를 수정합니다. 사용자가 현재 요청에서 명시한 매일 귀가·수면 권한을 목표에 저장하고, 일일 계획과 수면, 다음 날 계획을 각각 분리된 AI 호출로 연결합니다.

## 1.19.6 검증 결과

- Go 전체 테스트 통과. `set_goal_daily_life_policy` 등록, OpenAI 도구 노출, 수면 예약과 다음 날 실행 연결 계약을 확인했습니다.
- C# 회귀 검사 55개 통과. 명시적 귀가·수면 권한이 있는 미래 단계만 자동 수면 대상으로 선택되고 같은 날짜에는 재호출되지 않는지 확인했습니다.
- 실제 설치 게임 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- 인게임에서는 기존 돈 목표 프롬프트를 다시 실행해 정책 저장, 오늘 물주기, 자동 귀가·수면, 오전 안정화, 다음 날 자동 물주기의 연속 흐름을 확인해야 합니다.

## 1.19.5 검증 기록

1.19.5는 실제 인게임 로그에서 `goal_plan_build` 결과가 128KB를 넘어 `AI_TOOL_RESULT_TOO_LARGE`로 장기 목표 전체가 중단된 문제를 수정합니다. 기존 작물 성장일과 계획 단계 수를 제한하고, OpenAI 지침 누락 및 대형 결과의 복구 경로를 보완했습니다.

## 1.19.5 검증 결과

- Go 전체 테스트 통과. OpenAI가 장기 목표·경제 계획·단계 실행 규칙을 받는지와 대형 결과가 종료 오류 대신 작은 복구 관측으로 바뀌는지 확인했습니다.
- C# 회귀 검사 53개 통과. `99999` 성장일이 28일로 제한되고 65단계 저장 계획이 안전하지 않은 계획으로 판정되는지 확인했습니다.
- 실제 설치 게임 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- 인게임에서는 같은 장기 돈 목표로 계획이 64단계 이하에서 생성되고, 오늘 작업 뒤 `GOAL WAITING`으로 이어지는지 확인해야 합니다.

## 1.19.4 검증 기록

1.19.4는 다음 날 오전 6시의 기상 화면 전환이 끝나기 전에 0.5초 간격의 입력 8회를 모두 소진하고 `MORNING_INPUT_NOT_VERIFIED`를 반환하던 문제를 수정합니다. 날짜 변경 뒤 `CanMove=false`인 동안은 기다리고, 조작 가능 상태가 된 뒤 정상 이동 입력으로 `hasMoved`와 `shouldTimePass()`를 확인합니다.

## 1.19.4 검증 결과

- Go 수면 계약 검사와 전체 테스트를 통과했습니다.
- 실제 설치 게임 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- 인게임에서는 자동 수면 후 `[MORNING TRANSITION WAIT]`, `[MORNING CONTROLLABLE]`, `[MORNING INPUT]`, `[MORNING VERIFIED]` 순서와 오전 6시 이후 시간 진행을 확인해야 합니다.

## 1.19.3 검증 기록

1.19.3은 실제 인게임 로그에서 확인된 `Installed game data type unavailable` 계획 생성 실패를 수정합니다. Stardew Valley 1.6에서 별도 어셈블리로 로드되는 `CropData`와 `ShopData`를 전체 AppDomain 및 `StardewValley.GameData` 어셈블리에서 해석합니다. 미래 단계는 `TASK_BLOCKED` 대신 영속 `GOAL WAITING` 체크포인트로 종료하며, 계획기 실패 뒤 수동 농사로 우회하지 않습니다.

## 1.19.3 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 84개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `51B64B9E058A17B22254FABC4231E4051A806F0F49410EE67385A0EEA66DBC57`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사 51개와 Go 계약 검사가 통과했습니다. Go 검사는 `GOAL WAITING`의 정확한 판별과 GameData 어셈블리 탐색 계약을 포함합니다.
- 설치 게임의 `StardewValley.GameData.dll`에서 `StardewValley.GameData.Crops.CropData`와 `StardewValley.GameData.Shops.ShopData` 타입이 실제 존재하는 것을 확인했습니다.
- 실제 게임에서는 기존 작물 목표가 `build_goal_plan`을 통과하고, 오늘 물주기 뒤 다음 날 단계가 저장·자동 재개되는지 확인해야 합니다.

## 1.19.2 검증 기록

1.19.2는 인벤토리 판매 후보가 없을 때 농장의 기존 작물을 별도 수익 전략으로 계산합니다. 작물 위치, 수확물 ID, 남은 성장일과 예상 판매액을 관측하고 고정 구역의 물주기·수확·판매 계획을 만듭니다. `tend_existing_crops`는 이미 심어진 작물 관리만 허용하므로 씨앗 구매와 새 경작·파종 금지를 유지할 수 있습니다.

## 1.19.2 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 84개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `42BEC815DE1B6E674B798C471684B52257C4ED6E9387B69CAA6494D56473AB7C`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사 51개와 Go 계약 검사가 통과했습니다. 계약 검사는 기존 작물 전략, 전용 권한, 작물 상태 지문 및 실행 단계 권한 분리를 확인합니다.
- 실제 게임에서는 인벤토리가 비어 있고 살아 있는 작물이 있는 상태에서 새 씨앗 구매·파종 없이 기존 밭만 관리·수확·판매하는지 확인해야 합니다.

## 1.19.1 검증 기록

1.19.1은 판매 가능한 수확물이 없는 돈 목표를 일반 채팅의 `TASK_BLOCKED`로 끝내지 않고 전용 질문창으로 넘깁니다. 피에르 상점 영업 전에는 계획 단계에 오전 9시 재개 시각을 저장하고, 영업일이 아니거나 영업이 끝났다면 다음 날로 넘겨 자동 재개합니다. 사용자가 금지한 씨앗 구매와 새 농사는 질문 답변으로 명시적으로 허용하기 전까지 계속 금지합니다.

## 1.19.1 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 84개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `273B62E5A2FFF973042A1E3F85CA0F33E613DD4CC8FC797F3C62FDCD9D57F8F2`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사 51개 통과. 계획 단계의 `notBeforeTime=900` 저장·복원과 기존 schema 변환을 확인했습니다.
- Go 계약 검사는 빈 판매 후보의 전용 질문 전환과 영업 시작 시각 대기를 확인합니다.
- 실제 게임에서는 판매 수확물이 없는 경우 질문창이 뜨는지, 수확물을 가진 오전 6시대 목표가 오전 9시에 자동 재개되는지 확인해야 합니다.

## 1.19.0 검증 기록

1.19.0은 저장된 돈 목표 계획을 고유 lease가 있는 한 단계씩 실행합니다. 구매·경작·파종·물주기·수확·판매 결과를 실제 돈, 인벤토리와 고정 농장 영역에서 다시 검증하고 다음 날짜의 단계는 자동 재개합니다. 목표 저장 schema는 3이며 이전 실행 전 계획은 안전한 재계획을 한 번 요구합니다.

## 1.19.0 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 84개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `6C761395DE2BDA05BB887EC076554353B4056A90198F7EFC969E5E8FB08F5254`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사 50개 통과: schema 1→3 변환, schema 2 계획의 재계획 강제, 고정 밭과 진행 중 lease JSON 복원, 계획 면적 계산을 포함합니다.
- Go 계약 검사는 84개 도구 등록, 한 단계 lease, 완료 보고, 고정 밭 바인딩과 다음 날 자동 재개 연결을 확인합니다.
- 자동 검사는 실제 메뉴 이동과 여러 게임 날짜를 진행하지 않습니다. 구매·경작·파종·물주기·수확·판매와 게임 재실행 후 자동 재개는 `INGAME_TESTS_1.19.0.md`의 프롬프트로 확인해야 합니다.

## 1.18.0 검증 기록

1.18.0은 목표별 전략 선택, 날짜·의존관계가 있는 영속 계획, 경제 상태 지문 기반 계획 무효화, 전용 질문 답변에서 검증된 행동 권한 반영을 추가합니다. 계획은 실제 게임 행동을 실행하지 않습니다. 목표 저장 schema를 2로 올리고 기존 schema 1을 자동 변환합니다.

## 1.18.0 검증 결과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 80개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `616F60E92BF69DB04E4C96D1B2F1234BC9E168B9D77027C79C248C966C106AB9`.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- C# 회귀 검사는 전략 선호도 선택과 schema 1→2 변환을 포함한 45개입니다.
- Go 계약 검사는 실제 도구 80개와 계획 비실행·전용 질문 연계 계약을 확인합니다.
- 실제 게임 통합 확인에서는 계획 저장 후 재실행 복원, 돈/날짜/인벤토리 변경 시 `stale`, 질문 허용 후 재계획을 확인해야 합니다.

## 1.17.0 검증 기록

1.17.0은 기능 지원표, 경제 상태 통합 관측, 설치 데이터 기반 작물 수익 계산, 목표별 수익 후보와 `restore_tilled_soil`을 추가합니다. 경작지 복원은 빈 `HoeDirt`만 대상으로 곡괭이 정상 입력을 보내고 실제 제거를 검증하며 작물·객체는 제외합니다. 경제 분석은 읽기 전용이고 자동 전략 선택·실행은 하지 않습니다. C# 회귀 검사는 단일 수확·재수확·계절 내 수확 불가 계산을 포함한 41개를 실행하며, Go 계약 검사는 76개 실제 도구 등록과 정상 곡괭이 입력 계약을 확인합니다.

## 1.17.0 이번 PC에서 통과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l`, `go test ./...`, `go vet ./...` 통과. 등록된 실제 AI 도구 76개를 확인했습니다.
- Windows amd64 Go 서버 빌드 통과. SHA256: `407679652F5CE8DE00DE06DD87E098207819427AF8F5AB61EE8879384C8A2B45`.
- C# 정책·메모·지식·목표·경제 계산 회귀 검사 41개 통과.
- 실제 설치된 Stardew Valley/SMAPI 참조 C# 빌드 성공: 오류 0개, 기존 경고 8개.
- 실제 게임 실행 통합 검증은 별도입니다. 설치 후 첫 인게임 확인에서는 빈 경작지·작물 있는 경작지·일반 땅이 섞인 영역을 사용해 대상 제외와 상태 검증을 확인해야 합니다.

## 1.16.0 이하 검증 기록

1.16.0은 `goals-v1` 주·백업 저장, 돈 목표 상태 전이, 현재 잔액·순잔액 증가 검증, 마감 경과 판정, 관련 메모 문맥의 활성 목표 주입, 전용 사용자 질문창과 답변 후속 실행, 오른쪽 아래 진행 HUD를 추가합니다. C# 회귀 검사는 목표 계산·상태 전이·JSON 왕복을 포함한 38개를 실행하고, Go 계약 검사는 71개 실제 도구 등록과 장기 목표·전용 질문 UI 계약을 확인합니다.

1.15.0은 장기 메모 선별 주입, 세이브 로드·날짜 변경 복원 감사, 실제 맵·상점 입력 기반 지식 캐시 무효화, 구조화된 위키 지식, 나무 정상 입력 타격을 통합합니다. 정책 검사는 목표·장소별 메모 선별, 완료 작업 제외, 사용자 규칙 유지, JSON 왕복 뒤 revision·안정 ID 보존, 맵 입력 변화에 따른 SHA-256 서명 변경을 확인합니다. Go 계약 검사는 선택 메모가 한 번만 데이터로 주입되는지와 나무 타격이 정상 입력 분기를 사용하는지 확인합니다.

1.14.3은 나무 관측에 `treeState`, `isStump`, `hitsRequired`, `estimatedTotalHitsToRemove`, `removalSequence`를 추가합니다. 밑동을 성목으로 표시하던 `isFullyGrown`/`canBeChopped` 판정을 수정하고, 일반 `clear_target`이 나무를 한 단계만 처리한 뒤 완료로 보고하지 못하게 차단합니다. Go 회귀 테스트는 JSON 역직렬화부터 실제 AI 문맥 문자열까지 상태 정보가 보존되는지 확인합니다.

1.14.2는 다음 날 로드 후 `Game1.newDay`가 참인 6시 입력 게이트에서도 정상 이동 입력을 보내도록 수정합니다. 이전 구현은 이 값이 참이면 입력 전송 전에 반환했기 때문에 사용자의 클릭이 성공으로 오인될 수 있었습니다. 새 구현은 네 방향을 순환하며 입력하고 `hasMoved`와 `shouldTimePass()`를 함께 확인하며, `MORNING GATE/INPUT/VERIFIED` 로그를 남깁니다.

1.14.1 핫픽스는 일반 나무의 `growthStage`, `isFullyGrown`, `canBeChopped`를 AI 문맥에 명시하고, 과일나무를 `find_best_target(tree)` 후보에서 제외하며, 대상이 없는 `remove_wild_trees`를 `NO_ELIGIBLE_TREE`로 차단합니다. 실제 실패 좌표 `(43,13)`처럼 성장 단계 5 미만인 나무를 성목으로 오인하는 회귀 테스트를 추가했습니다.

이전 공유 대화의 다섯 가지 요구사항을 로컬 1.13.6 소스에 이어 구현했습니다. 이전 환경에서 작성했다는 1.14.0 소스/ZIP은 이 PC에 없어, 그 바이너리를 검증했다고 간주하지 않습니다.

## 이번 PC에서 통과

- `Verify.ps1 -GamePath "E:\SteamLibrary\steamapps\common\Stardew Valley"` 전체 실행 성공.
- `gofmt -l` 검사, `go test ./...`, `go vet ./...` 통과.
- Windows amd64 Go 서버 빌드 통과. SHA256: `3598FD0E61BF99E1D1C4F591CE958D38C9780CA3D21561192C5D556F6889D702`.
- 게임 코드와 공유하는 C# 정책·메모·지식 회귀 검사 30개 통과: 최단 경로/가중 경로 차이, 보호 벽, 현관 간선 제한, 경계 및 이미 도착한 상태, F7 취소, 에너지 부족, 야간 제한, 같은 날 시간 초과 반복 방지, 다음 날 재개, 충분한 회복과 미미한 회복 구분, 관련 메모 선별, 완료 작업 제외, JSON 왕복 복원, 맵 입력 변경 시 지식 서명 무효화.
- 실제 설치된 Stardew Valley/SMAPI 참조로 C# 모드 빌드 성공: 오류 0개, 경고 8개. 폐기 예정 WebSocket API, nullable, 기존 Count 메서드 그룹과 미사용 변수 경고 포함. 경고를 숨기지 않았습니다.
- 기존 Go 테스트의 농사·거래·상자·시간 알람·AI 공급자·치트 비노출 계약 유지.
- 로컬 게임 어셈블리의 LevelUpMenu/ShippingMenu 정상 확인 경로를 읽고 사용했습니다. LevelUpMenu.receiveLeftClick이 비어 있어 정상 okButtonClicked 처리 및 실제 입력을 사용합니다.

## 자동 테스트가 보장하지 않는 범위

- 실제 게임을 실행한 통합 테스트는 수행하지 않았습니다. 모드를 설치하거나 세이브를 수정하지 않았고 유료 AI API를 호출하지 않았습니다.
- `go test -race`는 이 PC에 CGO용 C 컴파일러가 없어 실행하지 못했습니다. 기존 문서의 과거 race 통과를 이번 실행 결과로 재사용하지 않았습니다.
- 배포 ZIP은 소스·검증 스크립트·Go exe를 포함하고, 게임 DLL·빌드 캐시·개인 설정·로그·세이브별 체크포인트는 제외합니다. C# 모드는 설치 시 게임 참조로 다시 빌드합니다.

## 인게임 확인 항목

1. 일반 스킬 상승과 배송 정산 화면을 연속 처리하고 저장 완료를 기다리는지. 직업 선택은 자동 선택 없이 명확히 중단하는지.
2. 다음 날 정상 입력으로 기상 대기가 해제되고 실제로 움직일 수 있는지. 알 수 없는 이벤트/메뉴는 임의로 닫지 않는지.
3. 여러 성목에서 한 그루씩 나무→밑동→드롭 순서를 지키는지. 드롭 개수와 인벤토리 증가가 일치하는지.
4. 먼 드롭 우선 수거와 짧은 장애물 제거 경로를 선택하는지. 보호 대상·도구 부족·장애물 예산·에너지/시간 제한을 지키는지.
5. 저에너지·시간·알람 중단이 세이브/플레이어별로 보존되고, 회복/다음 날 AI 재관측 후 원래 좌표만 재개하는지. F7 이후에는 자동 재개하지 않는지.
6. 기존 농사·상자 수량 보존·열기/닫기·F6 새 목표·F7 취소 회귀.

인게임 오류가 있으면 동일 행동을 반복하기보다 정확한 TASK_BLOCKED 결과와 로그를 보존해야 합니다.
